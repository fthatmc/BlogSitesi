@using BlogSitesi.Model.Entity
@model List<TblSocialMedia>

<div class="social-links text-right m-auto ml-sm-auto">
    @foreach (var x in Model)
    {

        <a href="@x.SocialMediaUrl" class="link"><i class="@x.Icon"></i></a>

    }
</div>
